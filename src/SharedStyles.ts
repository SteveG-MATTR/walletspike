import {StyleSheet} from 'react-native';

export const Colors = {
  primaryHighlight: '#2E29FC',
};

const Styles = StyleSheet.create({
  titleText: {
    fontWeight: 'bold',
    fontSize: 20,
    paddingHorizontal: 20,
    paddingBottom: 10,
  },
  paragraphText: {
    paddingHorizontal: 20,
  },
});

export default Styles;
